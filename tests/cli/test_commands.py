"""CLI integration tests — split into domain modules.

See:
  - test_issue_commands.py    (create, show, update, close, reopen, claim, comments, labels)
  - test_query_commands.py    (list, search, ready, blocked, stats, metrics, critical-path)
  - test_workflow_commands.py (types, transitions, validate, packs, guide, plan, batch, events)
  - test_admin_commands.py    (init modes, install, doctor, server, export/import, JSON retrofit)
"""
