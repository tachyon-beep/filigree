# Phase C Handover Prompt — Filigree 2.0 Loom Endpoints

**Branch:** `2.0-project-management-extension` (do not switch or create branches)
**Baseline commit:** `c021d4a` (Phase B close-out polish)
**Status:** Phase A + Phase B locally complete, unpushed. Phase C unstarted.

## Read in this order before touching code

1. `docs/architecture/decisions/ADR-002-api-generations-and-federation-posture.md` — the north star. Named generations, `classic` frozen, `loom` new in 2.0, MCP/CLI reflect living surface. **Read all of it.**
2. `docs/plans/2026-04-24-2.0-federation-work-package.md` — the full execution plan. **Phase C is your scope.** §B amendments and §B3/B4 deferrals are load-bearing context.
3. `docs/federation/contracts.md` — fixture pinning discipline (shape-reference, not byte-equality).
4. `tests/fixtures/contracts/loom/scan-results.json` — the C1 contract you implement against. The `shape_decl` block is the design-intent rationale.
5. `src/filigree/generations/loom/{types,adapters}.py` — the scaffolding from B2 you build on.

## What is in flight at handover

Eight unpushed commits on the branch (`aeec75e..HEAD`):

```
c021d4a docs(2.0): mark consumer-pinning sketch illustrative, not tested
ba3ca9b test(2.0 B4): parity-per-generation tests against contract fixtures
fca744e feat(2.0 B2): shape adapter scaffolding for classic + loom generations
df9a36a feat(2.0 B1): split classic + loom router factories per dashboard route module
36bc6f4 docs(2.0): reality-align Phase B plan before touching routing code
276c88b docs(2.0): A2 publish contract fixtures for classic + loom scan-results
c0e8f35 docs(2.0): A3 ADR-017 semantics audit — clean
d968df8 docs(2.0): land ADR-002 + federation work package; supersede prior plans
```

Pre-push checklist green throughout. 4029 pytest passed + 3 loom-parity skips (your C1 gate) + 1 pre-existing xfail (`test_container_key_parity` — your C2 gate).

## Phase C scope (in order, each is a single commit boundary)

### C1 — `POST /api/loom/scan-results`

The plan task says "identical request shape to classic; response wrapped in loom envelope; handler calls `db.process_scan_results(...)`; adapter wraps the result." Concretely:

1. In `src/filigree/dashboard_routes/files.py::create_loom_router()`, add `@router.post("/scan-results")` (full path `/api/loom/scan-results`) that:
   - Parses the same request body as the classic handler at `files.py:297` (the `api_scan_results` function). Reuse the validation logic — extract a private helper if duplication is bothering you, but **do not change the classic handler's behaviour**.
   - Calls `db.process_scan_results(...)` exactly as classic does.
   - Wraps the `ScanIngestResult` via `filigree.generations.loom.adapters.scan_ingest_result_to_loom(...)` and returns it.
2. **Living-surface alias decision (advisor-flagged).** `/api/scan-results` has no classic counterpart, so it's the unambiguous case for living-surface aliasing. Recommended: mount the same handler at `/api/scan-results` as well (loom + living surface, no classic conflict). Document the decision in `docs/federation/contracts.md` and add a docstring on the handler: `"Equivalent to /api/loom/scan-results as of 2026-04-26."`
3. Remove `@pytest.mark.skip` from `tests/util/test_generation_parity.py::TestLoomGenerationParityScanResults`. The three examples must pass — especially the one-finding-with-Clarion-metadata case (real adapter load test).
4. If you do the living-surface mount, add a small `TestLivingSurfaceEquivalenceScanResults` in `test_generation_parity.py`: hit `/api/scan-results` with the success example, assert the response body matches the response from `/api/loom/scan-results` for the same request (shape match, not byte). This converts B3's "deferred to per-endpoint Phase C resolution" into a precedent for later C tasks.
5. **Do not weaken** `tests/util/test_cross_surface_parity.py::TestScanResultsEnvelope::test_success_shape_empty_findings` — it's a stronger value-literal pin (`files_created == 0`) for classic that B4 parity does not subsume.

Gate before C2: classic `POST /api/v1/scan-results` unchanged (verify by re-running `TestClassicGenerationParityScanResults` and `TestScanResultsEnvelope`); loom skips flipped to passes; pre-push checklist green.

### C2 — `/api/loom/batch/update` and `/api/loom/batch/close`

1. Request body uses `issue_ids: list[str]` (loom vocabulary), not `ids`.
2. Response is `BatchResponse[SlimIssueLoom]`. `succeeded` is a list of `SlimIssueLoom` (loom shape with `issue_id`); `failed` is `list[BatchFailure]`; `newly_unblocked` (close only) is `list[SlimIssueLoom]`.
3. Add a loom-side adapter in `src/filigree/generations/loom/adapters.py`: `slim_issue_to_loom(issue: Issue) -> SlimIssueLoom`. Use the existing `SlimIssueLoom` TypedDict from `loom/types.py`.
4. The pre-existing strict-xfail at `tests/util/test_cross_surface_parity.py::TestBatchMixedValidityParity::test_container_key_parity` is your gate — it currently xfails because dashboard returns `{updated, errors}` and MCP returns `{updated, failed}`. Once loom batch endpoints exist, the xfail's assertion target moves to loom (which uses `succeeded/failed`); classic dashboard's `{updated, errors}` stays frozen. Update the xfail mark or convert to two tests (one classic, one loom).
5. Publish `tests/fixtures/contracts/loom/batch-update.json` and `batch-close.json` per the fixture pattern in `tests/fixtures/contracts/loom/scan-results.json`. Add `TestLoomGenerationParityBatchUpdate` and `TestLoomGenerationParityBatchClose` in `test_generation_parity.py`.

### C3 — `GET /api/loom/issues/{issue_id}` + single-issue CRUD

1. Path uses `{issue_id}` (loom vocabulary).
2. Response is `IssueLoom` (define in `src/filigree/generations/loom/types.py`). `issue_id` replaces `id` at every level.
3. `include_files` query param defaults to `False` per the plan §C3 (classic default remains `True`).
4. Add adapter `issue_to_loom(issue: Issue) -> IssueLoom`.
5. Add the loom-CRUD endpoints (`POST /api/loom/issues`, `PATCH /api/loom/issues/{issue_id}`, `POST /api/loom/issues/{issue_id}/close`, `POST /api/loom/issues/{issue_id}/reopen`, `POST /api/loom/issues/{issue_id}/claim`, `POST /api/loom/issues/{issue_id}/release`, `POST /api/loom/claim-next`, `POST /api/loom/issues/{issue_id}/comments`, `POST /api/loom/issues/{issue_id}/dependencies`, `DELETE /api/loom/issues/{issue_id}/dependencies/{dep_issue_id}`). Each gets a fixture and a parity test.

### C4 — `/api/loom/*` list endpoints

`list_issues`, `search_issues`, `list_files`, `list_findings`, `list_observations`, `list_scanners`, `list_packs`, `list_types`, `get_ready`, `get_blocked`, `get_comments`, `get_issue_events`, `get_issue_files`, `get_changes`. All return `ListResponse[T]` with `items`, `has_more`, optional `next_offset`. Classic counterparts (bare arrays) preserved unchanged.

Each list endpoint gets its own loom-side TypedDict and adapter. Fixture per endpoint; parity test per fixture.

### C5 — `response_detail` on loom batch endpoints

Add `response_detail: Literal["slim", "full"] = "slim"` query param to every loom batch endpoint from C2 + C3. `"slim"` returns `SlimIssueLoom`; `"full"` returns full `IssueLoom`. Classic batch endpoints **do not** gain this param.

Gate before Phase D: every operation exposed by classic has a loom counterpart at `/api/loom/*`; classic endpoints unchanged from 1.x; all loom fixture parity tests pass; the `test_container_key_parity` xfail flipped or split.

## Operational rules (carry from Phase A/B)

- **Plan amendment before code change** for any conflict between the plan text and code reality. The Phase B amendment (commit `36bc6f4`) is the precedent; replicate the pattern.
- **Pre-push checklist before every commit:** `uv run ruff check src/ tests/`, `uv run ruff format --check src/ tests/`, `uv run mypy src/filigree/`, `uv run pytest --tb=short`. All four must be green.
- **Do not push, switch branches, or create branches without explicit user approval.**
- **Each Phase C task is a logical commit boundary.** Bundled-on-branch per the work-package's option 2c; revertable per-commit.
- **Adapters are thin and data-only.** No `if generation == ...` branches inside adapter modules. If you need conditional logic, it lives in the handler.
- **Observations are ambient.** If you notice an issue while working, fire `mcp__filigree__observe` and carry on.

## Carryover notes from Phase B close-out (advisor-flagged)

1. **B4 parity is shape-only.** `_assert_shape_matches` does `type(actual) is type(expected)`, not value equality. Adapter unit tests are where you assert specific values. Keep the existing classic value-literal pin in `TestScanResultsEnvelope::test_success_shape_empty_findings` untouched.
2. **Living-surface aliases land per-endpoint as Phase C lands each endpoint** — not as a phase-wide sweep. Document each decision (alias vs. classic-only) in `docs/federation/contracts.md` as you make it.
3. **Skip-flipping is your real B4 validation.** Each `pytest.mark.skip` removed in Phase C is a verification that the harness actually catches adapter bugs.

## Definition of done for Phase C

- [ ] `POST /api/loom/scan-results` mounted; adapter wraps `ScanIngestResult` per fixture; loom skips flipped to passes (C1).
- [ ] Loom batch endpoints (`update`, `close`) mounted with `issue_ids` input + `BatchResponse[SlimIssueLoom]` output; container-key xfail resolved (C2).
- [ ] Loom single-issue CRUD endpoints mounted with `issue_id` vocabulary + `IssueLoom` shape; `include_files` default `False` (C3).
- [ ] Loom list endpoints all return `ListResponse[T]` envelopes (C4).
- [ ] `response_detail` param on loom batch endpoints (C5).
- [ ] Every loom endpoint has a fixture under `tests/fixtures/contracts/loom/` and a parity test in `tests/util/test_generation_parity.py`.
- [ ] Classic endpoints byte-identical at the wire surface (verify by classic parity tests + the existing `TestScanResultsEnvelope` value pin).
- [ ] Living-surface aliases adopted per-endpoint where unambiguous; documented in `docs/federation/contracts.md`.
- [ ] Pre-push checklist green; zero new strict-xfails introduced; the existing container-key xfail resolved or migrated to a per-generation form.

## Start point

C1, step 1. Begin by reading the classic `api_scan_results` handler at `src/filigree/dashboard_routes/files.py:297`, then the loom adapter at `src/filigree/generations/loom/adapters.py::scan_ingest_result_to_loom`, then the contract fixture. Implement C1, run the gate, commit, then move to C2.
