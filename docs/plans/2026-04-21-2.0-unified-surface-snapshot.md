# 2.0 Unified Surface — Implementation Snapshot

**Date:** 2026-04-21
**Branch:** `2.0-project-management-extension`
**Status:** Stage 1 + Stage 2a locally complete; pre-push bug burndown underway; Stage 2B to be replanned before resuming.

Companion to `2026-04-18-2.0-unified-surface-design.md` and `2026-04-18-2.0-unified-surface-plan.md`.

---

## Progress

- **Stage 1 — Envelope + ErrorCode foundation (7 commits)**
  - Envelope `TypedDict`s added
  - `ErrorCode` enum introduced
  - Flat `ErrorResponse` shape landed
  - Typed exceptions migrated onto the new shape
- **Stage 2a — Sweep to uppercase ErrorCode everywhere (14 commits)**
  - Fixture bootstrap for error-envelope assertions
  - Legacy-code mapping dict established
  - 8 sweeps across `src/filigree/mcp_tools/*`
  - Unmapped-code fix-up pass
  - Dashboard error flattening
  - CLI `--json` output aligned to uppercase `ErrorCode`

**21 commits on branch, 0 pushed.**

## Final state (local)

| Check | Result |
| --- | --- |
| `pytest` | 3951 / 3951 green |
| `mypy` (whole tree) | 0 errors (was 136 mid-Stage-1; Stage 2a cleared them) |
| `ruff check` | clean |
| `ruff format --check` | clean |

All MCP tools, dashboard routes, and CLI `--json` output now emit uppercase `ErrorCode` values.

## Intentional deferrals

- `BatchFailureDetail` in `src/filigree/db_issues.py` — **Stage 2b.0 will retire this type.**
- `TransitionError` in `src/filigree/mcp_tools/common.py` — wire-case question flagged in observation `filigree-obs-daed45efe0`.

## Observations filed (awaiting review)

| ID | Topic | Disposition |
| --- | --- | --- |
| `filigree-obs-daed45efe0` | `TransitionError` wire-case question | Deferred |
| `filigree-obs-f55f1ce22c` | Task 1.5 mypy gate was aspirational | Resolved by Stage 2a completion |
| `filigree-obs-87b797747e` | Schema-mismatch message triplication | Scheduled for Stage 5 cleanup |
| `filigree-obs-d7d258c747` | 8 unmapped MCP codes mapped by judgment (`rate_limited → IO` most debatable) | Review |
| `filigree-obs-ab1f775df4` | 15 unmapped dashboard codes mapped by judgment (`INVALID_TYPE`, `GRAPH_INVALID_PARAM`, `TREE_LOAD_ERROR` most debatable) | Review |

## Current decision point

Autonomous run was paused at the Stage 2a / Stage 2B boundary. The next move is one of:

1. **Push** Stage 1 + Stage 2a as one combined PR.
2. **Push** Stage 1 and Stage 2a as two separate PRs.
3. **Continue** into Stage 2B locally without pushing (breaking-envelope rollout across ~28 files, with revert-readiness concerns).

The user has chosen a fourth path: **pause stage work, run a major bug burndown, then replan/rebaseline Stage 2B against requirements.** Stage 2A execution was rough enough that the original Stage 2B plan is no longer trusted as-is.

## Next actions

1. Work through the bug burndown on-branch; keep Stage 1 + 2a commits intact.
2. After burndown, revisit `2026-04-18-2.0-unified-surface-design.md` and `...-plan.md` against the current envelope shape and any learnings from 2a.
3. Produce a rebaselined Stage 2B plan (separate file, dated) before resuming the unified-surface rollout.
4. Defer the push-vs-bundle decision until the rebaseline is done — no shared-state publishing until then.

## Context to carry forward

- Stage 2a's legacy-mapping dict and the two observation threads (`-d7d258c747`, `-ab1f775df4`) are the empirical ground truth for what "uppercase ErrorCode everywhere" actually cost. Any Stage 2B replan should start from that mapping, not from the original design's assumed code inventory.
- `BatchFailureDetail` retirement is a prerequisite hop (Stage 2b.0) before the breaking-envelope rollout; keep it at the front of the replanned sequence.
- Revert-readiness was the main concern that made Stage 2B feel risky. The replan should make the revert story explicit per-file or per-surface, not defer it to execution time.
